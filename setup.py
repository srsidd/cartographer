import sys
from distutils.core import setup

setup(name='cartographer',
      version='0.0.1',
      description='Python client library to resolve the lat, lng coordinates for a given address',
      scripts=[],
      url='https://github.com/srsidd/catographer',
      packages=['cartographer'],
      # test_suite='nose.collector',
      # tests_require=['nose'],
      license='Apache 2.0',
      platforms='Windows'
      )
